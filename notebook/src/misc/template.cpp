#include <bits/stdc++.h>

#define FOR(i, n) for(ll i = 0; i < (ll)(n); ++i)
#define ALL(x) (x).begin(), (x).end()

#define pb push_back
#define mt make_tuple
#define mp make_pair
#define fi first
#define se second

#define X(A) get<0>(A)
#define Y(A) get<1>(A)
#define Z(A) get<2>(A)
#define W(A) get<3>(A)

#define DEBUG(X) { cerr << #X << " = " << (X) << endl; }
#define outSET(X) { cerr << "set " << #X << " = "; for (auto _:X) cerr << _ << ' '; cerr << endl; }
#define outVEC(X) { cerr << "vector "<< #X << " = "; for (auto _:X) cerr << _ << ' '; cerr << endl; }
#define outMAP(X) { cerr << "map " << #X << " = "; for (auto _:X) cerr <<'('<<_.fi<<' '<<_.se<<')'<<' '; cerr << endl; }
#define debugfunc(f,x) printf("%s(%g) = %g\n",#f,(x),(f(x)));
#define debugfunc2(f,x,y) printf("%s(%g,%g) = %g\n",#f,(x),(y),(f((x),(y))));

using namespace std;

using ll = long long int;

using vi = vector<ll>;
using vvi = vector<vi>;
using vb = vector<bool>;
using vvb = vector<vb>;

using pi = pair<ll, ll>;
using ppi = pair<pi, ll>;

int main(void)
{
    ios_base::sync_with_stdio(0);
    #ifndef check_script
        freopen("PB_NAME.in", "r", stdin);
        freopen("PB_NAME.out", "w", stdout);
    #endif

    return 0;
}
